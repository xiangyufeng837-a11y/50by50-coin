# Coin Flip Application

This simple Python application simulates a fair coin toss with a 50/50 probability.

## Usage

```bash
python coin_flip.py
```

The script uses the `secrets` module to generate a cryptographically secure random choice, ensuring unbiased results.
